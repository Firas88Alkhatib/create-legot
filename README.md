# Create Legot Application

A CLI to create a new [Legot](https://www.npmjs.com/package/legot) application.

Get started by running:

```bash
$ npm create legot@latest
```

and then follow the prompt.
